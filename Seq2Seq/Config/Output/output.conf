[Logging]
embedding_log_path = embeddings/log
training_log_path = results/log

[Dumping]
vocab_path = vocab
embedding_checkpoint_path = embeddings/trained_model
embedding_matrix_path = embeddings/embedding.matrix
training_checkpoint_path = results/checkpoints
